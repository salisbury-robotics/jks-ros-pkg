#include "Parameters.h"

using namespace std;

ostream &operator<<(ostream &stream, const Parameters &p)
{
    return stream;
}

istream &operator>>(istream &stream, Parameters &p)
{
    return stream;
}
